# 2026-09-17 现场收音日志诊断

只读检查服务器 `/opt/julia-runtime/control-v1-20260910/live.log`、当前源码及最近保存的输入 WAV；未修改服务器、重启或烧录。

## 已确认

- 本地 sdkconfig 和 build/config/sdkconfig.h 已为 FFT 开启；固件构建时间 2026-09-16 23:59:44。此前“当前关闭”的说法已不适用于现在的工作区。
- 服务器在 2026-09-17 00:00:27 收到设备新连接。协议未上报 FFT 开关/固件哈希，单凭服务器日志不能证明设备运行的是该构建。
- 00:03:55.655，utterance 27：15.00 s，reason=limit。
- 00:04:14.913，utterance 28：9.92 s，reason=silence。
- 00:04:34.970，utterance 29：12.34 s，reason=silence。
- 后续 utterance 56、57 也到 15 秒上限。

## 已保存录音按相同 320 点统计口径重算

逐帧去均值、周期 Hann、单边 PSD；flatness≤0.199 且 entropy∈[0.070,0.809]；联合活跃还要求原始 RMS 大于该段冻结底噪+3 dB。
这些是上传音频的离线重算结果，不是设备直接上报的 FFT 计数，也没有逐帧语音/噪声标签。

| utterance | 时长/结束 | FFT通过 | 能量且FFT通过 | 最后3秒联合通过 |
|---|---|---:|---:|---:|
|27|15.00 s / limit|82.80%|51.60%|58.67%|
|28|9.92 s / silence|86.49%|58.87%|36.00%|
|29|12.34 s / silence|89.30%|51.38%|27.33%|
|54|7.02 s / silence|90.03%|47.01%|28.00%|
|55|9.04 s / silence|86.95%|50.00%|36.67%|
|56|15.00 s / limit|86.67%|50.00%|56.67%|
|57|15.00 s / limit|86.13%|68.27%|75.33%|

27 段冻结底噪 −73.6613 dBFS，维持阈值约 −70.6613 dBFS；录音逐帧 RMS 中位数 −69.89 dBFS。
56 段冻结底噪 −73.6011 dBFS，逐帧 RMS 中位数 −69.91 dBFS。
这解释了为什么很多较低电平的帧仍能满足相对能量门限，而宽人声频谱候选包络也没有排除它们。

普通对话仍采用“通过帧抵扣40 ms、不通过帧累计20 ms”的原尾部规则。当通过帧不断穿插出现，静音累计很容易被抵扣；约三分之一的平均通过比例就可抵消平均增量，但是否触发700 ms还取决于帧排列，不能仅凭比例精确预测结束点。
目前数据说明：即便已开启 FFT，这些现场音频也能大量通过，频谱筛选不足以保证及时收尾。不能简单解释为没有调用 FFT，也不能把所有通过帧称为噪声。

## 其它现象

当前云端人声复核调用 `speech_segments` 抛出 NotImplementedError，`SpeechDetector.is_speech` 异常分支返回 True（fail-open）。因此这道复核没有有效排除非人声；它在段结束后运行，不是15秒收尾延迟的直接原因。
同时还有 PCM2 序列异常（23:53）、ping_timeout（23:59）和 TTS RuntimeError（00:04:38），需要与录音拖尾分开诊断。
服务器当前源码已不同于9月15日快照，不能用旧文档推断当前人声复核路径。

## 下一步依据

优先核实设备构建/开关，并用带人工停说时刻的现场样本评估尾部累积规则和频谱阈值。现有正样本包络不是人声分类器，不宜仅继续收紧同一组阈值；起音与尾部可考虑分别标定，但本次未修改算法。
数值证据保存为同目录 `FFT_GATE_LIVE_STATS_20260917.jsonl`；原音频留在服务器，未下载。
