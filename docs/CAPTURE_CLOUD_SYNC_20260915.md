# capture-v1 云端内容同步

2026-09-15。按用户最新指令，仅完成内容同步和 Git 记录，后续验证由用户执行。

- 原地同步目录：`/opt/julia-runtime/control-v1-20260910/release`，没有新建部署目录。
- 此目录原先不含 Git；已初始化 Git，并仅纳入迁移涉及的源码及现成测试，未纳入配置、凭据或运行日志。
- 分支：`codex/capture-v1`。
- 基线提交：`fd4eaac`。
- 回退标签：`backup/pre-capture-v1-20260915`。
- 修改提交：`ce1cce1`。
- 五个修改后源码按统一 LF 时的 SHA-256 与已测试本地副本一致。
- **没有执行服务重启，也没有刷机。** 用户要求停止后，未再执行联调或验证；当前只确认磁盘内容已同步。

已有服务控制入口：`/opt/julia-runtime/control-v1-20260910/start.sh`。后续若要完整加载同步的代码，需要执行该入口的 `restart`；本次未执行。

本地算法和协议说明见 [LOCAL_CAPTURE.md](LOCAL_CAPTURE.md)。其中早先“未部署”的记录表示当时状态，以本页同步状态为最新记录。
