# 验证与验收

文档版本：V1.1。本文给出执行方法与记录要求，不将未执行的硬件项目标为通过。返回 [项目入口](../README.md)。

## 1. 验证范围与状态

2026-08-31 的当前工作区已完成 ESP-IDF 5.5.4 固件构建，以及 `build-ota-name/julia_fused_base.bin` 的离线镜像检查：项目名 `julia_fused_base`、应用版本 `0.1.0`、文件大小 1943856 字节，镜像内部校验有效。该记录只对应当时的工作区和构建产物，不能替代任意 Git 提交的重建结果。

[主机回归测试](../tests/host/README.md) 的 9 个 CTest 目标于 2026-09-03 通过，覆盖 PCM 缓冲、播放控制、FSM、业务链路断开、WSS 写入、上行 ring／pump、会话恢复和显示所有权。它们模拟 RTOS／I2S／TLS／时间，不覆盖物理 DMA、真实网络或多核压力。应用入口、语音服务和播放模块还通过本地唤醒宏关闭服务器模式时的条件编译语法检查；该检查不代表完整本地 WakeNet 模型烧录与运行验收。

验证记录按范围区分：

| 范围 | 已有结果 | 不代表的结果 |
| --- | --- | --- |
| 固件 `build-ota-name/` | ESP-IDF 构建、镜像名／版本／大小及内部校验通过 | 不代表已刷入设备或完成音质、续航验收 |
| 编辑器目录 `build/` | reconfigure 通过，编译数据库与配置头存在；入口、播放、PCM 缓冲、语音服务、网络生命周期和板级音频共 6 个源文件按该数据库通过语法检查 | 不代表该目录已经完成链接并生成镜像 |
| 主机目录 `build-host/` | 9 个 CTest 目标通过，具体目标见[主机回归测试](../tests/host/README.md) | 不代表真实 DMA 时序、多核压力或网络联调通过 |
| 本地唤醒条件分支 | 3 个修改模块的宏配置语法检查通过 | 不代表完整模型镜像已构建或本地唤醒率达标 |

本版未包含完整上板、服务器联调或功耗测试的通过记录。以下表格是执行清单，不是已经通过的测试报告。仓库的 `check_lex_balance.py`、`verify_comments_only.py` 是辅助脚本，不构成端到端测试套件。

## 2. 测试记录

每次运行记录：源码提交号及未提交差异、SDK／工具链版本、配置文件摘要、镜像 SHA-256、板卡版本、device_id、供电、串口、服务器版本、网络条件和测试时间。

涉及语音时，记录音量、距离、背景噪声、音频样本编号和唤醒模式；涉及性能时，同时保留原始样本、样本量、失败数以及 P50／P95。凭据与用户原始语音不得直接写入公开记录。

结果使用“通过／失败／未执行／不适用”；已知缺陷不能通过修改预期值变为通过。文档中的数值默认是当前配置或格式推算，除非明确标为测量结果。

## 3. 构建与镜像

| 编号 | 操作 | 核对内容 |
| --- | --- | --- |
| BUILD-01 | 在独立构建目录执行完整 build | 配置、编译、链接、bootloader、镜像与分区大小检查完成 |
| BUILD-02 | 用 esptool 查看 image_info，并计算整个 bin 的 SHA-256 | 芯片、project_name、应用版本、实际长度、校验有效 |
| BUILD-03 | 在独立配置副本中设置不匹配的 OTA 镜像名 | CMake 一致性检查拒绝生成；不污染日常配置 |
| BUILD-04 | 使用另一台已配置工具链的机器构建 | 无未说明的本机路径依赖；记录依赖和工具版本 |
| BUILD-05 | 对照 ESP-IDF buildPath 与 C/C++ compileCommands | 均指向 build；真实编译条目包含新源文件和当前 sdkconfig 目录 |

## 4. 启动与显示

| 编号 | 场景 | 核对内容 |
| --- | --- | --- |
| BOOT-01 | 正常冷启动 | 80MHz、25%背光下显示→音频→存储顺序启动，Wi-Fi最后启动；初始化完成后 S0→S3并等待唤醒词；记录各阶段耗时与电压 |
| BOOT-02 | 无 Wi-Fi、错误密码或服务端不可达 | 本地显示可工作；记录各连接的重试行为，不将失败连接视为启动成功 |
| BOOT-03 | 显示初始化失败、网络很快取得 IP | 关键显示初始化失败进入 S7.2；Wi-Fi仍在本地运行时处理之后启动，网络回调不能提前开放交互 |
| BOOT-03A | 电池供电冷启动，分别启用显示、音频和 Wi-Fi | 串口依次出现 `power_hold`、`ota_ready`、`display_ready`、`audio_ready`、`storage_ready`、`runtime_ready`、`wifi_started`、`wifi_settled`；记录最低电压和首次缺失阶段，禁止插 USB 掩盖压降 |
| BOOT-04 | 板级音频、语音服务或 FSM 初始化失败 | 待验证新镜像应回滚；已确认镜像保存 `julia_fault` 快照并按 S7.2 策略处理 |
| BOOT-05 | 启动后 MQTT/WSS 持续不可用超过初始连接期限 | `CONNECTING` 超时后只进入一次 S7.1并播放本地提示；随后 S3 显示 `offline`，服务全部恢复后标签消失 |
| BOOT-06 | 连续制造相同关键初始化故障 | 每次记录递增 sequence；核对不会被误分类为普通网络故障 |
| UI-01 | S4 中有效话语 MIC_START → MIC_STOP → SPKS／PCM → SPKE | MIC_START 保持 S4；MIC_STOP 进入 S2.2；正常 SPKS 进入 S2.3；实际播完回 S1 |
| UI-01A | S3/S5/S6 收到 `wake_detected` | 先提交 S4 并回匹配 `interaction_id` 的 `state_ready`；唤醒回应期间闭眼底图上的独立嘴型随 PCM 动作；播完闭嘴并仍保持 S4 |
| UI-01B | 收到 MQTT `intent_result` | `normal` 不改变状态；`goodnight` 零语音进入 S6；`dismiss` 零语音进入 S5，背光固定为配置值（默认 50%）；随后 `MIC_STOP` 不再推进对话 |
| UI-02 | 非 busy 陪伴达到 `CONFIG_JULIA_DISPLAY_SLEEP_TIMEOUT_SECONDS`（默认 600 秒） | 从 S1 的 50% 固定背光进入 S3 待机、闭眼与 5%–30% 背光呼吸；同时检查 MIC 仍按服务器模式上传 |
| UI-02A | S3 连续驻留达到 `CONFIG_JULIA_STANDBY_SLEEP_TIMEOUT_SECONDS`（默认 300 秒） | 投递 `EVT_STANDBY_TIMEOUT` 并进入 S6，停止呼吸、背光熄灭且面板进入睡眠；中途唤醒会取消旧计时 |
| UI-02B | S5 连续驻留达到 `CONFIG_JULIA_SILENT_STANDBY_TIMEOUT_SECONDS`（默认 1800 秒） | 投递 `EVT_SILENT_TIMEOUT` 并回到 S3；中途唤醒会取消旧计时 |
| UI-03 | 听音、思考或播放超过普通闲置阈值 | 不被普通闲置策略抢占；业务等待超时按已知限制记录 |
| UI-04 | 有效 RTC／SNTP，覆盖 22 点、23 点、07 点 | 22 点事件当前不迁移；夜间宽限进入 S6；07 点不旁路点亮，仍等待唤醒词离开 S6 |
| UI-05 | 无效 RTC／未同步时间 | 夜间调度不依赖无效墙钟误触发 |
| UI-06 | S6 中轻触、桌面振动和明显搬动设备 | 调试门限0.20g／25°/s、200ms采样下，单次轻触不触发；连续运动约800ms后投递 `EVT_MOTION_WAKE`，FSM执行S6→S3并统一唤醒面板、闭眼待机和背光呼吸；不得直接操作显示 |
| UI-07 | OTA 任务成功、普通任务失败、链路失败、严重本机故障 | 分别验证 S8→S0、S8→S3、S8→S3、S8→S7.2；忙碌态拒绝准入时保持原状态 |
| UI-08 | 连续观察左右眼半闭／全闭／睁眼循环 | 闭眼时底图下缘无残留；睁眼恢复原坐标；上下边缘无新的接缝 |
| UI-09 | 依次覆盖 S0～S8、S2.1／S2.2／S2.3、S7.1 和 S7.2 | `(60,100)` 黑色状态码完整显示且始终置顶；断联后红色 `offline` 跨主状态保留，相关连接恢复后消失；严重故障显示 `S7.2 FAULT` |

## 5. 语音与协议

| 编号 | 场景 | 核对内容 |
| --- | --- | --- |
| VOICE-01 | 默认模式建立 WSS | 未发 MIC_START 前已有 PCM1，界面保持待机 |
| VOICE-02 | 接收一段 PCM1 | 魔数、载荷长度、序号、字节和正确；记录缺口，不假设跨会话清零 |
| VOICE-03 | MIC_STOP 后观察上行 | 仍有 PCM1，进入思考；不得按“麦克风静音”验收 |
| VOICE-03A | 输入固定声压的测试音，检查 AFE 与 PCM1 | 两路都应用 70% 数字增益；峰值无削波，PCM1 dBFS 与缩放后样本一致 |
| VOICE-04 | 16kHz／24kHz 播放 | 顺序为 SPKS → 偶数字节 PCM → SPKE；音量、声音和嘴型合理 |
| VOICE-05 | 未开播 PCM、奇数字节 PCM、超长消息 | 不把非法音频送入播放；连接关闭与忽略行为按协议记录 |
| VOICE-06 | 首包／播放中暂停 1 秒，再继续 PCM；另测 15 秒无数据 | 短暂停顿继续播放；15 秒且缓冲为空时明确超时，中止后回到可交互状态 |
| VOICE-07 | 播放时 MIC_START，并注入迟到 SPKE | 待播数据清空、旧完成事件不覆盖 LISTEN；测量真实停播延迟；另测新 SPKS 后迟到旧 SPKE 的协议限制 |
| VOICE-08 | 服务器持续心跳但不返回回答 | 检查是否长期停在 THINK；当前没有业务超时兜底 |
| VOICE-09 | WSS 断开并恢复 | 上传／播放停止和重连正确；确认 UI、队列与首批新音频行为 |
| VOICE-09A | 持续 MIC 上行期间由服务器发送带 code/reason 的 CLOSE | 板卡优先读取并回显完整 CLOSE 载荷；服务器能观察到正常关闭而非仅 TCP 异常断开；达到 `CONFIG_WSS_CLOSE_WAIT_MS`（默认 500ms）仍未完成时设备强制清理并重连 |
| VOICE-09B | 服务端覆盖 2.29～2.34 秒及 3.5 秒预算附近的真实发送背压，并确认设备出现 WANT_READ／WANT_WRITE／EAGAIN | 同一写入区间有限重试并记录 recovered，不因暂时错误重连；约 5.12 秒 PSRAM ring 吸收积压，服务恢复后 PCM 序号连续 |
| VOICE-09C | ring 中存在未发送 PCM 时强制断开并重连 WSS | 旧 generation 全部丢弃；新连接从空 ring 和连接建立后的新采集帧开始，绝不重放旧唤醒词或尾音 |
| VOICE-09D | 限制服务端读取直至 256 帧 ring 满 | producer 关闭当前代次但不清ring/TLS；WSS owner 记录 `audio_overflow`、统一 teardown，FSM 进入 S7.1 并播放本地提示，三秒后按来源策略返回；新连接不发送任何旧帧 |
| VOICE-09E | 分别断开 WSS、MQTT，同时覆盖 S1～S6 | 每轮 `ONLINE→OFFLINE` 只进入一次 S7.1并播放一次本地语音，嘴型随实际播放 PCM变化；保持离线期间重复断联不再播报。S3/S5/S6 返回原状态，S1/S2/S4 返回 S3；快速重连也不得恢复 S1 的旧免唤醒语义；对应链路全部恢复后标签消失，S8 不受该业务迁移影响 |
| VOICE-10 | MQTT 单命令及不支持命令 | 只有三类作业可执行；不等待不存在的 vstatus 回执 |
| VOICE-11 | 本地唤醒配置单独构建／烧录 | 模型分区、识别与采音链路有效；不得仅凭服务器模式构建认定通过 |
| VOICE-12 | 服务端一次发送超过 64KiB 未消费数据 | 显式 playback_overflow、清空缓冲并回到可交互状态；服务端按播放速率节流 |
| VOICE-13 | 短音频、缓冲积压后 SPKE、重复 SPKE | 所有已接收 PCM 有序播放，尾音不截断；正常完成后才清 busy |

## 6. 文件与 OTA

| 编号 | 场景 | 核对内容 |
| --- | --- | --- |
| FILE-01 | 外发存在的 WAV | BEGIN 大小、实际字节数和 END 一致；内容可独立校验 |
| FILE-02 | 无卡、文件不存在、错误扩展名、超限文件 | 正确拒绝；不格式化用户卡 |
| FILE-03 | 读取中断或连接中断 | 不接受截断文件；服务器接收超时和设备收尾有记录 |
| FILE-04 | 文件发送期间 MIC_START／SPKS／SPKT | 收到 file_cancelled 后放弃部分文件；后续 MIC PCM1 不再被当作文件字节 |
| OTA-01 | MQTT 连接与 critical SUBACK | 发起 ota_check，身份及当前版本正确 |
| OTA-02 | 合法／缺少 schema_version 的 ota_notify | 合法通知触发检查；缺少 schema_version 的通知被拒绝 |
| OTA-03 | update=false 与合法升级清单 | 身份、request_id 和清单必填字段符合协议 |
| OTA-04 | 不匹配身份、过期时间、错误摘要或镜像名 | 拒绝安装；不能通过 force_update 绕过非版本校验 |
| OTA-05 | 完整 HTTP 200 下载 | 实际字节、镜像头、SHA-256、启动分区和状态正确 |
| OTA-06 | 下载中断后返回 206 | 校验 Content-Range／ETag、恢复偏移与最终镜像哈希，合法 206 不应隔离发布物 |
| OTA-07 | Range 请求返回 200／416 | 记录是否从零下载；不得算作 206 续传通过 |
| OTA-08 | 测试镜像启动失败、无网络、可回滚／不可回滚 | 本地验收、回滚／安全模式与报告关联正确 |
| OTA-09 | 报告期间断网、重复 PUBACK、队满 | event_id 幂等；容忍重复和中间状态缺失，不误判安装完成 |

故障注入使用专用测试设备和镜像，保留串口恢复方法；不能在用户设备上随意断电、损坏存储或烧写安全 eFuse。错误摘要与版本示例不发布给正常设备。

## 7. 性能与功耗指标

| 指标 | 测量定义 |
| --- | --- |
| 首屏时间 | 上电／复位到第一幅稳定可见画面 |
| 可对话时间 | 上电／复位到语音会话可接收有效用户话语 |
| 唤醒漏检率 | 已标注唤醒样本中未成功进入听音的比例，分距离与噪声统计 |
| 误唤醒率 | 无目标唤醒词音频中的误触发次数／小时 |
| 回答首音延迟 | 用户话语结束到扬声器首个可听回答；设备／服务端阶段分别打点 |
| 打断停播延迟 | 用户插话开始或服务器确认到实际声音停止，注明采用哪个起点 |
| 上行丢帧 | PCM1 seq 缺口、入队失败和连接切换分开统计 |
| 播放稳定性 | 首包延迟、断流次数、最长停顿、播放恢复方式 |
| 内存与调度 | 内部 RAM／PSRAM 余量、最大连续块、任务栈余量、队列高水位 |
| 上行追赶 | `peak_frames`、`drain_ms`、同连接恢复次数、`audio_overflow` 会话终止次数 |
| 显示性能 | 实际刷新时间／帧率、撕裂和嘴型偏移，不用任务周期代替 |
| 状态功耗 | 在固定电源、电压、亮度、网络与音量下测各状态平均及峰值功耗 |
| 电量显示 | 用校准电源覆盖3.0～4.2V，验证正常黑色BAT、15%进入红色LOW、25%恢复回差；以10秒周期模拟原始电压累计上升20mV并确认2次后绿色CHG覆盖LOW，相对峰值下降20mV确认2次或60秒无上升证据后退出CHG，若仍低电量则恢复LOW；开机阶段采样不得触发CHG，只有电压无效时隐藏，带载结果不得宣称为库仑计或STAT精度 |

产品目标需在规定测试条件下确定；本版不提供未经测量的性能达标值。平均功耗按各状态实际驻留比例加权，电池容量还需考虑转换损耗、可用容量和峰值电流，不能只按待机电流估算。

## 8. 发布门槛

- 构建、镜像属性和目标硬件匹配，配置与产物可追踪。
- 核心启动、正常对话、停止／打断、断网恢复和 OTA 完整安装有实际记录。
- 已知问题逐项标注影响、适用场景及是否阻止发布；不得隐藏 Range 恢复或业务超时限制。
- 生产认证、服务器身份、凭据、用户采音告知与数据保护有明确配置和验证。
- 不以“编译成功”“MQTT PUBACK”或“已写入 OTA 分区”替代整机运行与安装成功。
