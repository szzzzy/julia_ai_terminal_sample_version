# 通用代码注释规范（2026）

## 1. 核心原则

> **代码说明“怎么做”，注释说明“为什么这样做、必须满足什么约束、这样做意味着什么”。**

变量名、函数名和代码结构应负责表达程序行为。

注释的主要价值，是补充代码本身无法充分表达的信息，例如：

* 设计意图；
* 约束和不变量；
* 方案选择原因；
* 异常处理语义；
* 参数来源；
* 并发和资源所有权；
* 跨模块契约；
* 非直观行为。

### 总原则

> **假设读者已经理解编程语言和基本 API，但不了解当前设计的背景和决策。**

不要教读者“代码在做什么”，而要帮助读者理解：

> **为什么必须这样做，以及哪些东西不能随便改变。**

---

# 2. 禁止复述代码

如果删除注释以后，读者仍然能够从变量名、函数名或代码结构直接得到完全相同的信息，则该注释通常没有价值。

### 错误示例

```c
// 检查麦克风是否开启
if (isMicActive) {
    ...
}
```

```c
// 缓冲区大小增加1
bufferSize++;
```

```c
// 调用音频处理函数
processAudio();
```

这些内容已经由代码表达。

### 推荐写法

```c
// 只有通话期间才处理音频，结束后立即停止后台计算。
if (isMicActive) {
    processAudio();
}
```

注释应补充代码没有表达出的设计意图。

---

# 3. 不机械翻译变量名、函数名和宏名

注释不是标识符的中文翻译。

### 错误示例

```c
// isReady表示系统是否准备完成
bool isReady;
```

```c
// 调用processData处理数据
processData();
```

### 原则

行内注释原则上不重复当前代码中已经清楚出现的：

* 变量名；
* 函数名；
* 类型名；
* 宏名。

但以下情况可以明确引用标识符：

* API 文档；
* 跨模块依赖；
* 调用顺序要求；
* 生命周期约束；
* 接口契约；
* 必须精确定位某个公共对象。

例如：

```c
// 必须在 network_init() 完成后调用，否则证书存储尚未初始化。
```

此时函数名本身就是接口契约的一部分，明确写出反而更准确。

---

# 4. 一条注释表达一个逻辑单元

不要把多个互不相关的动作、条件和设计决定压缩在同一条注释中。

### 不推荐

```c
// 检查缓冲区，如果有数据就读取，然后处理，如果设备忙就等待后重试
```

### 推荐

```c
// 缓冲区为空时跳过本轮处理。

// 每次只取一个完整数据块，避免消费者看到部分更新的数据。

// 外设繁忙时主动让出CPU，避免忙等待。
```

但不要机械地按照逗号或顿号拆分。

例如：

```c
// 连接断开后清除历史数据，避免重新连接后重放旧内容。
```

这是一个完整的“动作 + 原因”逻辑，应保留在一起。

---

# 5. 优先注释“不变量”

复杂系统中最有价值的注释之一，是说明：

> **无论程序如何执行，都必须始终成立的条件。**

例如：

```c
// 一个数据块只有完整处理成功后才能释放。
```

```c
// 新会话不得继续使用上一会话留下的数据。
```

```c
// 只有资源所有者可以执行销毁操作。
```

这些信息往往无法仅通过局部代码可靠推断，却直接决定整个系统是否正确。

对于以下代码尤其应考虑记录不变量：

* 状态机；
* 多线程；
* 中断；
* 网络协议；
* 缓冲区；
* 生命周期管理；
* 数据库事务；
* 缓存；
* 异步任务。

---

# 6. 并发代码应说明资源所有权

涉及多线程、任务、中断、异步回调时，应优先说明：

* 谁创建资源；
* 谁负责写入；
* 谁负责读取；
* 谁可以修改状态；
* 谁负责销毁；
* 哪些上下文禁止访问；
* 是否需要同步。

例如：

```c
// 采集线程是唯一生产者，发送线程是唯一消费者。
```

```c
// 连接生命周期仅由网络线程管理，其他线程只能发送控制请求。
```

```c
// ISR只写入事件标志，实际资源释放由任务上下文完成。
```

如果所有权关系已经由类型系统或架构完全约束，则无需重复说明。

---

# 7. 异常注释重点说明“错误意味着什么”

错误处理代码通常很容易看出：

```c
if (error) {
    ...
}
```

真正需要解释的是：

> 为什么这个错误采用当前恢复策略？

### 不推荐

```c
// 出错后重试
```

### 推荐

```c
// 当前错误只表示资源暂时不可用，不代表连接已经失效，因此保留现场后重试。
```

或者：

```c
// 数据完整性已经无法保证，本轮结果作废，不继续处理残缺输入。
```

异常路径的注释优先说明：

* 错误性质；
* 是否可恢复；
* 为什么重试；
* 为什么放弃；
* 为什么回退到某个状态；
* 为什么不能恢复原状态。

---

# 8. 参数注释说明“为什么是这个值”

不要重复常量本身。

### 不推荐

```c
// 阈值为80
#define THRESHOLD 80
```

### 推荐

```c
// 根据典型环境实测结果保留约20%的噪声裕量。
#define THRESHOLD 80
```

参数注释最好能够说明其来源属于哪一类：

* 协议规定；
* 硬件限制；
* 理论计算；
* 实验结果；
* 性能权衡；
* 兼容性要求；
* 临时经验值。

例如：

```c
// 当前值来自早期实验，用于功能验证，正式版本需要重新标定。
```

这种说明非常重要，可以防止临时参数以后被误认为正式设计指标。

---

# 9. 明确单位和时间基准

所有无法从类型和名称中可靠判断单位的量，都应明确单位。

常见包括：

* ms / us / s；
* tick；
* Hz；
* byte / bit；
* sample / frame；
* V / mV；
* A / mA；
* °C；
* dB；
* 百分比；
* 时间戳。

如果涉及时间，还应在必要时说明时间基准，例如：

* 相对启动时间；
* 单调时钟；
* 系统时间；
* UTC；
* 本地时间。

不要让读者猜：

```c
uint32_t timeout = 2000;
```

中的 `2000` 到底是什么。

优先通过变量名、类型或常量本身表达单位；无法表达时再由注释补充。

---

# 10. 保留必要的领域术语

不要为了追求“全中文”而强行翻译已经具有明确行业含义的术语。

例如：

* FreeRTOS
* Task
* Queue
* Semaphore
* ISR
* DMA
* GPIO
* SPI
* I2C
* UART
* TCP
* MQTT
* HTTP
* PCM
* VAD
* Codec
* Callback
* Thread
* Heap
* Stack
* Ring Buffer
* Bootloader
* Watchdog

推荐：

```c
// DMA完成后再开放缓冲区，避免消费者读取尚未搬运完成的数据。
```

不必将 `DMA` 强行翻译成长句。

原则是：

> **保留能够提高精确性的专业术语，其余解释使用自然语言。**

---

# 11. 注释直接表达逻辑

避免大量背景铺垫。

### 不推荐

```c
// 由于多线程程序的调度具有不确定性，而且系统可能在任意位置发生线程切换，
// 为了保证不同线程访问共享数据的时候不会出现各种异常情况，
// 所以这里需要进行同步处理。
```

### 推荐

```c
// 读写共享状态时持有互斥锁，避免线程切换产生不一致结果。
```

注释应尽可能做到：

> **读第一遍就能知道为什么存在。**

---

# 12. 不记录普通修改历史

Git 已经负责保存代码历史。

以下内容通常不应长期存在于代码中：

```c
// 原来这里使用方案A，后来改成了方案B。
```

```c
// 2025年某人修改了这里。
```

```c
// 上一版本有bug，所以这样写。
```

这些内容应该进入：

* Git commit；
* Issue；
* Pull Request；
* ChangeLog；
* ADR；
* 设计文档。

只有当历史原因仍然构成当前约束时，才值得保留，例如：

```c
// 当前运行环境不支持该接口，因此仍使用兼容实现。
```

这里真正重要的不是历史，而是**当前仍然存在的限制**。

---

# 13. TODO 必须具有可执行含义

避免：

```c
// TODO: 优化
```

```c
// TODO: 看看
```

```c
// TODO: 以后处理
```

推荐：

```c
// TODO: 收集低信噪比场景数据后重新标定检测阈值。
```

或者：

```c
// TODO: 当底层接口支持异步写入后，删除当前轮询兼容逻辑。
```

好的 TODO 应至少说明：

> **什么条件满足后，需要做什么。**

---

# 14. 不为简单代码逐行配字幕

避免：

```c
// 创建对象
...

// 初始化对象
...

// 检查结果
...

// 返回成功
...
```

这种注释会显著降低代码信噪比。

简单、直观、符合惯例的代码不需要注释。

> **没有注释，比无价值注释更好。**

---

# 15. 注释应解释非直观设计

当代码看起来“可以写得更简单”，但实际不能简单写时，应说明原因。

例如：

```c
// 不在回调中直接释放资源，避免与仍在执行的异步任务发生生命周期竞争。
```

这类注释非常重要，因为后来的维护者很可能会认为：

> “这里为什么这么绕？我简化一下。”

好的注释应该能够阻止这种错误重构。

---

# 16. 文件、函数和局部注释职责不同

## 文件级注释

主要说明：

* 模块职责；
* 模块边界；
* 关键依赖；
* 核心不变量。

不要在文件头重复整个实现流程。

---

## 公共 API 注释

主要说明：

* 输入；
* 输出；
* 前置条件；
* 返回值；
* 所有权；
* 生命周期；
* 是否阻塞；
* 是否线程安全；
* 可调用上下文。

---

## 复杂函数注释

主要说明：

* 整体策略；
* 为什么采用这种实现；
* 核心步骤；
* 特殊约束。

无需逐句翻译函数内部代码。

---

## 代码块注释

说明：

> 为什么这一块必须这样处理。

---

## 单行注释

仅在局部行为明显违反直觉、存在特殊约束或需要解释参数依据时使用。

---

# 17. 注释必须与代码保持一致

过时注释比没有注释更危险。

修改以下内容时，应同步检查关联注释：

* 状态条件；
* 参数；
* 返回值；
* 调用顺序；
* 所有权；
* 生命周期；
* 异常策略；
* 协议字段。

如果无法保证注释长期同步，应减少对具体实现细节的描述，更多记录稳定的设计意图和约束。

---

# 18. 注释价值优先级

当需要决定某段代码是否值得注释时，可以按照以下优先级判断：

1. **不变量和安全约束**
2. **资源所有权和生命周期**
3. **设计选择原因**
4. **异常语义和恢复策略**
5. **参数依据**
6. **跨模块契约**
7. **兼容性限制**
8. **非直观行为**
9. **代码具体行为**

越靠后的内容，越应该优先尝试通过代码本身表达。

---

# 19. 提交前审查清单

提交代码前，可以逐条检查：

* [ ] 删除这条注释后，代码是否已经表达完全相同的信息？
* [ ] 是否只是在翻译变量名、函数名或宏名？
* [ ] 是否说明了设计意图，而不是代码语法？
* [ ] 是否存在需要记录的不变量或边界条件？
* [ ] 并发资源的所有权是否明确？
* [ ] 异常恢复策略是否说明原因？
* [ ] 参数是否说明来源或依据？
* [ ] 单位和时间基准是否明确？
* [ ] 是否包含已经失效的历史信息？
* [ ] TODO 是否具有明确触发条件和目标？
* [ ] 是否存在逐行“字幕式注释”？
* [ ] 注释是否与当前代码保持一致？
* [ ] 专业术语是否使用准确，而不是为了中文化强行意译？
* [ ] 一个注释是否承担了过多互不相关的信息？

---

# 20. Agent 自动补注释时的额外要求

使用 AI 或代码 Agent 自动增加、修改注释时，应额外遵守：

1. 不为了提高“注释覆盖率”而增加注释。
2. 简单代码保持无注释。
3. 优先从上下文推断设计意图，但无法确定时不要编造原因。
4. 无法确认参数来源时，不得虚构“实测”“协议规定”等依据。
5. 不得把函数名、变量名重新翻译成自然语言充当注释。
6. 不得根据代码结果反向杜撰设计目的。
7. 修改逻辑时同步更新相关注释。
8. 优先补充模块边界、不变量、异常语义和所有权。
9. 实现细节已经由代码清楚表达时，不额外解释。
10. 无法确定设计意图时，宁可不写，也不要生成看似合理但未经确认的说明。

---

# 一句话总结

> **好的注释不是代码的中文配音，而是设计决策的最小必要说明。**

代码已经告诉读者“程序做了什么”。

注释真正应该告诉读者的是：

> **为什么这样做、什么条件必须保持成立，以及未来修改时哪些边界不能破坏。**
